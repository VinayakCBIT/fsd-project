function redirectToYouTube() {
  
  window.location.href = 'https://youtu.be/WlKU5lAJHhQ?feature=shared';
}
/*

const fullWidthImg = document.getElementById('full-width-img');
let currentIndex = 0;
const totalImages = 3; // Replace with the actual number of images

function showImage(index) {
  const translateValue = -index * 1147; // Assuming each image has a width of 1147 pixels
  fullWidthImg.style.transform = 'translateX(' + translateValue + 'px)';
}

function nextImage() {
  currentIndex = (currentIndex + 1) % totalImages;
  showImage(currentIndex);
}

function prevImage() {
  currentIndex = (currentIndex - 1 + totalImages) % totalImages;
  showImage(currentIndex);
}
*/
function redirectToReddit() {
    // Replace 'https://www.reddit.com/' with your actual Reddit link
    window.location.href = 'https://www.reddit.com/';
  }
  


